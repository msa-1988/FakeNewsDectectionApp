This folder stores the dataset used by the fake-news project.

Layout:
- `raw/News.csv`: tracked training dataset

The Streamlit app does not require the dataset at runtime once the trained
artifacts have been generated, but the training script does.
